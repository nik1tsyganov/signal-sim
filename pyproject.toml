[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "signal-sim"
version = "0.1.0"
description = "Paper-only fixture operate loop. No live broker."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
include = ["signal_sim*"]
